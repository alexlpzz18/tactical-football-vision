# Entorno virtual y Python
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd

# Anaconda
.conda/

# Datos y modelos (archivos demasiado grandes para Git)
data/raw/
data/processed/
models/weights/
data/tracking/
# cachés y CSVs del benjamín (viven en Drive)
data/tracking_benja/
data/annotations/

# Outputs generados
outputs/

# Jupyter
.ipynb_checkpoints/

# Variables de entorno y secretos
.env

# Mac
.DS_Store

# IDEs
.vscode/settings.json
# Notebooks de experimentos (pesados, valor documental para TFG, solo en local)
notebooks/

# Imágenes de calibración (pesadas, van en Drive/DVC)
# Frames de calibración (pesan y viven en Drive). El JSON de clics y la
# homografía SÍ se versionan: son la calibración del campo, no un dato.
data/calibracion/*.png
data/calibracion/*.jpg
data/calibracion_benja/*.png
data/calibracion_benja/*.jpg

# Modelos entrenados (van por DVC, no a GitHub)
*.pt

# Informes generados (derivados, se regeneran con el script)
data/reports/
